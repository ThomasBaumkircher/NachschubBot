services:
  nachschub_bot:
    image: python:3.11.1-slim
    environment:
      - BOT_KEY=${NACHSCHUB_API_KEY}
    restart: always
    build: ./NachschubBot
    command: python3 main.py

  ballkoenig_bot:
    image: python:3.11.1-slim
    environment:
      - BOT_KEY=${BALLKOENIG_API_KEY}
    restart: always
    build: ./BallkoenigBot
    command: python3 main.py
